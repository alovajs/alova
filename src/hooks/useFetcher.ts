// import { RequestState } from '../../typings';
// import Method from '../methods/Method';
// import sendRequest from '../functions/sendRequest';

// /**
// * 获取请求数据并缓存
// * @param method 请求方法对象
// */
// export default function fetch<S extends RequestState, E extends RequestState, R, T>(method: Method<S, E, R, T>) {
//  sendRequest(method, true);
// }