---
'alova': patch
---

Align package metadata and README with the official site messaging: update `description` and `keywords` in package.json, and refresh the root README (en/zh/ja) and npm README with the hero tagline, a `usePagination` code example, and a React Query comparison hook.
